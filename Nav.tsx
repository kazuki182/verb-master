import { verbs } from '@/data/verbs'
export default function Tests(){ const qs = verbs.flatMap(v=>v.tests.map(t=>({...t, verb:v.word}))).slice(0,10); return <div className="space-y-5"><div className="no-print"><h1 className="text-2xl font-semibold">テスト</h1><p className="mt-1 text-sm text-gray-500">A4 PDF保存はブラウザの印刷から「PDFに保存」を選択。</p><button onClick={()=>{}} className="mt-4 rounded-full bg-[#1f4e5f] px-5 py-3 text-white">印刷 / PDF保存</button></div><section className="print-card rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-xl font-semibold">穴埋めテスト</h2><ol className="mt-5 list-decimal space-y-5 pl-5">{qs.map((q,i)=><li key={i}><p className="text-lg">{q.q}</p><p className="mt-1 text-xs text-gray-400">Answer: {q.answer}</p></li>)}</ol></section><script dangerouslySetInnerHTML={{__html:`document.querySelector('button')?.addEventListener('click',()=>window.print())`}} /></div> }
